git config --global user.name
git config --global user.email

Update Git User Configuration

git config --global user.name "NewUsername"
git config --global user.email "NewEmail@example.com"

git config --global --list

******************
Check Saved Credentials (for HTTPS)
If you're using HTTPS, check the credentials stored in your system using the Git credential manager:

For Windows:
git credential-manager-core get

git credential-manager-core erase

Set New Credentials
Next time you perform a Git operation (e.g., git push), Git will prompt you for a username and password. Enter:

Username: Your GitHub username.
Password: Your GitHub Personal Access Token (PAT) (instead of your password).
